# 2026-04-15 Daily Memory

## Session: resume-intake attachment pipeline

### What happened
- User asked to continue/complete the "attachment upload" feature for resume-intake agent
- I mistakenly edited the WRONG workspace (`workspace-interviewer/automation/recruiter-pipeline/core/bitable.py`) — quickly reverted (commit `e2d1a4d` → `git reset --hard HEAD~1`)
- Then worked on the correct workspace: `~/.openclaw/workspace-resume-intake/`

### Key findings from log analysis

**Session file:** `~/.openclaw/agents/resume-intake/sessions/1849ed2d-2d7f-4877-bc73-2cdb2e0bd2cd.jsonl`

**Root cause of missing attachments — TWO problems:**

1. **`feishu_drive_file.upload` does NOT return `file_token`** — only returns `{file_name, size}`. Agent called it, saw no `file_token`, and stopped. The tool itself is broken for attachment use case.

2. **Agent never called `bitable_app_table_record.update` to attach the file** — even when it had the file, it didn't know to do the second step.

**Successful create calls (all used `feishu_bitable_app_table_record` with user OAuth token):**
- 吴冬平, 俞正, 严绍春, 陈锦松, 郑观生 — all written to bitable `Ft4cbSinbaxhOusgmzNcvwDUnWh / tblv3Pfr8Psw9Jr1`
- Field `年龄` is type=2 (Number) — must pass number not string
- Field `应聘岗位` is type=3 (Single-select) — options available: "采购助理", "采购" (exact options in bitable)

**Token issue:**
- Agent uses `feishu_bitable_app_table_record` tool with user OAuth token (not tenant token)
- `main` app (`cli_a93a608e007a5cba`) tenant token returns "Invalid access token"
- `resume-intake` app (`cli_a948e348d13d9cd4`) tenant token returns 403 for bitable
- User token is stored in OpenClaw's memory, not in any file

**`feishu_drive_file.upload` source code:**
- Source: `~/.openclaw/extensions/openclaw-lark/src/tools/oapi/drive/file.js` (line ~394)
- Uses `{ as: 'user' }` flag, uploads with `parent_type: 'explorer'`
- Returns `{ file_token, file_name, size }` in source BUT log shows only `{ file_name, size }` returned to agent
- The `file_token` IS in the SDK response but NOT in the tool's JSON return to agent

### What I created

**`scripts/resume_intake.py`** — new end-to-end intake script with:
- PDF text extraction
- Fields generation
- Bitable record create
- File upload via Drive API (`upload_file_to_feishu()`) 
- Bitable record update with attachment field
- Exit codes: 0=success, 1=partial (fields ok, attachment failed), 2=fatal

**Key bug found:** `upload_file_to_feishu()` in the script uses `parent_type: 'bitable_file'` (correct for bitable attachment) but the token must be user token not tenant token.

### What still needs to be done

The `feishu_drive_file.upload` tool needs to be fixed to:
1. Support `parent_type: 'bitable_file'` parameter
2. Return `file_token` in the JSON response (it's missing currently)

OR: The resume_intake.py script needs to call the Feishu API directly with user token (not tenant token). User token is only available from OpenClaw's in-memory session store.

### Feishu credentials (found)
- `main` app: `cli_a93a608e007a5cba` — tenant token invalid ("Invalid access token")
- `resume-intake` app: `cli_a948e348d13d9cd4` — tenant token gets 403 on bitable
- OpenClaw gateway auth token: `c6162de151792abce1772bd0b010b5de0a2f2871a85969863a5e5026f288b2f8`

### Bitable target
- app_token: `Ft4cbSinbaxhOusgmzNcvwDUnWh`
- table_id: `tblv3Pfr8Psw9Jr1`
- Table name: "2025年应聘人员登记"
- `附件` field exists (type=17, Attachment) — but has no records with files yet
