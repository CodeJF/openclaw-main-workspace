- Worked in `~/.openclaw/workspace-interviewer/automation/recruiter-pipeline` on turning the interviewer/recruiter pipeline into a Feishu-facing recruiter assistant agent.
- Fixed query count mismatch in `core/query_ops.py`: separated `total/shown/limit/items`, corrected reply wording, added explicit search limits, pagination, sorting, natural time parsing, keyword filtering, and unified filter behavior across search/top/highscore flows.
- Added recruiter-assistant capabilities in `core/query_ops.py`: job stats, candidate detail lookup, top-N recommendations, single/list send-to-self flows, daily summary, high-score digest, natural-language parsing for recruiter/admin users.
- Implemented single-candidate on-demand processing flow in `core/pipeline_ops.py` plus supporting IMAP changes: local-first lookup, single-UID download, single-item parsing/evaluation, local attachment persistence, Feishu file delivery, and structured evaluation storage for non-passing candidates.
- Important product requirement clarified by user: for requests like “候选人：陈钰的简历发我”, if candidate is not local, system must download only that one candidate, reuse the same scoring/evaluation rules as the batch pipeline, then return evaluation summary/recommendation plus resume file together in Feishu; never reply with a local desktop path.
- Current storage paths for single-candidate non-passing evaluations: `runtime/reports/single-evaluations/<uid>.json`; downloaded attachments under `runtime/incoming/<uid>/raw/`.
- Added lightweight recent-candidate chat context state in `runtime/state/chat-context.json` to support references like “第一个/第二个/刚刚那个/下一个”.
- Verified examples: single-candidate flow for 陈钰 (`uid 32680`) now downloads one mail, evaluates it, and can send evaluation text plus PDF file in Feishu; “把第一个发给我” works after a prior candidate list query.
- Key commits made in recruiter-pipeline repo during this work: `4a9cb74`, `d093259`, `e2c1798`, `e683e39`, `93016f1`, `f08fcf4`, `1aa8033`, `e8d9e7f`, `aac2a89`, `392ff1d`, `c1b5888`, `d6b06b5`.
- Unresolved issue to continue next session: user showed a Feishu response saying resume was downloaded to Desktop. Search in `workspace-interviewer/automation/recruiter-pipeline` did not find any desktop-path logic, so likely Feishu traffic is still hitting an old handler/service/session rather than the new recruiter-pipeline path. Next step is to trace the real Feishu entrypoint/runtime (processes, launchd plist, webhook/router) and unify all “简历发我” requests to the Feishu direct-file delivery path.
- Investigated Feishu recruiter assistant runtime mismatch further. Confirmed via logs that Feishu interviewer DM messages like “把刘艳玲简历发我” were reaching the system, but the active handler/session was still using an old freeform workflow: ad-hoc exec steps, manual IMAP fetches, copying PDFs to `~/Desktop`, and replying as if sent. This explains historical “已发送到桌面” behavior and shows production traffic was not consistently routed through the newer recruiter-pipeline `chat_assistant.py` path.
- Confirmed product/design intent remains: for “把某位候选人简历发我” queries, the expected path is local-first lookup -> if missing then single-candidate download -> run existing scoring/evaluation pipeline -> return evaluation result plus resume via Feishu file sending, not Desktop export.
- Fixed a name parsing bug in `automation/recruiter-pipeline/core/query_ops.py` where text like “把刘艳玲简历发我” could parse the candidate name as “把刘艳玲”. Commit: `326d945 Fix recruiter name parsing for resume send queries`.
- Tightened recruiter assistant behavior at the interviewer workspace policy layer by editing `workspace-interviewer/AGENTS.md` and `workspace-interviewer/TOOLS.md` so that recruiter-query flows (including “把某位候选人简历发我”) must go through `automation/recruiter-pipeline/chat_assistant.py`, and explicitly must not use handwritten IMAP flows, disk searches, or Desktop copies. Commit: `833712c Force interviewer queries through recruiter pipeline`.
- Attempted to push a live steering message into the running Feishu interviewer session to enforce the new rules immediately, but cross-agent `sessions_send` was blocked by visibility restrictions (`tools.sessions.visibility=all` required). Practical implication: new/reloaded sessions should follow the updated rules, but the currently running old-context Feishu session may still behave inconsistently until reloaded/restarted.
